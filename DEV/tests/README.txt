# Tests directory
Set up python unit tests here. Should be pretty simple.

NOTE: This should ONLY be reserved for unit testing.

## Syntax
Name each file according to test_<file-to-test>.py.
This allows unit tests to be separated from 'external' tests (ie. testing that a
plugin is valid).
